-- PeppermintEmotes OpenEmote compatibility module.
-- The main emote wheel owns the active UI; this module intentionally exposes
-- a safe callable without restoring the former account/repository branding.
return function(...)
    return true
end
